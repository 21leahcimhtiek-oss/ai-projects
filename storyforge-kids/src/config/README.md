# StoryForge Kids - Configuration

This directory contains configuration files for the StoryForge Kids application.

## Files

- `app.config.ts` – Expo app configuration (bundle IDs, permissions, plugins)
- `theme.config.js` – Theme color definitions for light/dark modes